<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demoimagesconveter">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoImagesConveter">
        <activity android:name=".view.Gallery.EditImageActivity"></activity>
        <activity android:name=".view.SldieShow.AddAudioActivity" />
        <activity android:name=".view.SldieShow.SlideshowActivity" />
        <activity android:name=".view.SldieShow.PrepareSlideshowActivity" />
        <activity android:name=".view.Gallery.GalleryActivity" />
        <activity android:name=".view.Setting.SettingActivity" />
        <activity android:name=".view.Capture.CaptureImageFromVideo" />
        <activity android:name=".view.Capture.ChooseVideoActivity" />
        <activity android:name=".view.Capture.OpenVideoFolders" />
        <activity android:name=".view.MainActivity">

            <!-- main intent filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>